import React from 'react';
import DataLoader from '../../SharedFile/DataLoader';

const BlogsPage = () => {
    return (
        <div>
            <DataLoader/>
        </div>
    );
};

export default BlogsPage;